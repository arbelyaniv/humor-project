## Supabase Schema

The Supabase table schema used for the project is stored in:

- `supabase_schema.sql`

Use this file to understand the table structure when querying Supabase.


The Supabase credentials are already set in environment variables.

- SUPABASE_URL is stored in .env.local
- SUPABASE_ANON_KEY is stored in .env.local